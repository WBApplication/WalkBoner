<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fusoft.walkboner">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/OneUI4Theme"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthActivity" />
        <activity android:name=".PermissionsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".CreatePostActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ModerationActivity" />
        <activity android:name=".BanUserActivity" />
        <activity android:name=".ModChatActivity" />
        <activity android:name=".PremiumActivity" />
        <activity android:name=".PinSetupActivity" />
        <activity android:name=".UnlockAppActivity" />
        <activity android:name=".FullPhotoViewerActivity" />
        <activity android:name=".AddInfluencerActivity" />
        <activity android:name=".AppDebugActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".PasswordResetActivity" />
    </application>

</manifest>